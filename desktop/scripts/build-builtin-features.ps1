# Skip-intro is generated at runtime (no shipped packs).

Campaign helpers live under repo `mods/wh3/ttw_campaign_helpers/` as a standalone MCT mod
and are **not** packaged with the manager. See `mods/README.md`.
